<?xml version='1.0' encoding='utf-8'?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-configuration-3.0.dtd">

<hibernate-configuration>
    <session-factory>
        <!-- Cấu hình kết nối database -->
        <property name="connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="connection.url">jdbc:mysql://localhost:3306/library_db?useSSL=false&amp;serverTimezone=UTC</property>
        <property name="connection.username">root</property>
        <property name="connection.password">password</property>

        <!-- Cấu hình connection pool -->
        <property name="connection.pool_size">10</property>

        <!-- SQL dialect cho MySQL -->
        <property name="dialect">org.hibernate.dialect.MySQL8Dialect</property>

        <!-- Bật quản lý session tự động của Hibernate -->
        <property name="current_session_context_class">thread</property>

        <!-- Tắt second-level cache -->
        <property name="cache.provider_class">org.hibernate.cache.internal.NoCacheProvider</property>

        <!-- Hiển thị SQL queries ra console -->
        <property name="show_sql">true</property>
        <property name="format_sql">true</property>

        <!-- Tự động tạo/cập nhật database schema -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- Mapping các Entity classes -->
        <mapping class="com.library.entity.Book"/>
        <mapping class="com.library.entity.Author"/>
        <mapping class="com.library.entity.Member"/>
        <mapping class="com.library.entity.Borrowing"/>
        
         <!-- Driver và URL kết nối đến MySQL -->
        <property name="hibernate.connection.driver_class">com.mysql.cj.jdbc.Driver</property>
        <property name="hibernate.connection.url">jdbc:mysql://localhost:3306/library_db</property>
        <!-- Thông tin đăng nhập -->
        <property name="hibernate.connection.username">root</property>
        <property name="hibernate.connection.password">password</property>
        <!-- Dialect tương ứng với MySQL -->
        <property name="hibernate.dialect">org.hibernate.dialect.MySQL8Dialect</property>
        <!-- (Các thiết lập tuỳ chọn) -->
        <property name="hibernate.show_sql">true</property>
        <property name="hibernate.format_sql">true</property>
    </session-factory>
</hibernate-configuration>